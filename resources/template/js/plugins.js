/* RPGAtlas plugin manager configuration.
   Add plugins here following the format:
   { name: "PluginName", status: true, description: "…" }

   Built-in plugins can be added via the editor's Plugin Manager. */

window.PLUGIN_CONFIG = [];