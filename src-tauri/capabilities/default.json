{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core + dialog access for the editor and play-test windows.",
  "windows": ["main", "playtest"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
